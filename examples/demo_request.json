{
  "schema": "CREATE TABLE users (name TEXT, country TEXT)",
  "question": "List user names from Canada",
  "execute": true,
  "setup_sql": [
    "CREATE TABLE users (name TEXT, country TEXT)",
    "INSERT INTO users VALUES ('Alice', 'Canada'), ('Bob', 'US')"
  ],
  "max_rows": 20,
  "timeout_ms": 1000
}
